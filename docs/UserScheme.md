# Документация

## User experience
Схема, показывающая опыт взаимодействия пользователя с ботом.
```mermaid
graph TD
  A[Главное меню] --> B[RAG]
  B --> |Проверка| C[Выбрать\nдиалог]
  C --> D[Новый]
  C --> E[Существующий]
  C --> U[Удалить\nдиалог]
  U --> F1[Выбрать\nколлекцию]
  F1 --> H[Запрос на\nудаление]
  D --> F[Выбрать\nколлекцию]
  F --> G1[RAG-ЗАПРОС]
  E --> G2[RAG-ЗАПРОС]
  A --> I[Поиск]
  I --> |Проверка| J[Выбрать\nколлекцию]
  J --> K[ПОИСК-ЗАПРОС]
  A --> L[Коллекции]
  L --> M[Посмотреть\nколлекции]
  M --> Z[Выбрать коллекцию]
  L --> |АДМИН| N[Добавить\nколлекцию]
  Z --> |АДМИН| O[Удалить\nколлекцию]
  Z --> |АДМИН| P[Загрузить документы\nв коллекцию]
  Z --> |АДМИН| Q[Удалить документ\nиз коллекции]
  Q --> R2.1[Посмотреть документы]
  Z --> R1.1[Посмотреть документы]
  R1.1 --> R1.2[ЗАПРОС]
  R2.1 --> R2.2[ЗАПРОС]
  N --> R2[ЗАПРОС]
  O --> R3[ЗАПРОС]
  P --> R4[ЗАПРОС]
  A --> S[Оплатить подписку]
  S --> T[WIP]
  
```